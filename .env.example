# Copiá este archivo como .env y completá tu key real de Groq.
# El archivo .env NO se sube a git (está en .gitignore).
# Tu key está en Vercel → Project → Settings → Environment Variables → GROQ_API_KEY
GROQ_API_KEY=gsk_tu_key_aca
